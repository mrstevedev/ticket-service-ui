export enum ROUTE {
    HOME = "/",
    EVENTS = "/events",
    EVENT = "/event/:id",
    MY_TICKETS = "/my-tickets"
}

export enum API_ENDPOINT {
    BASE_URL = "http://localhost:3000"
}

export enum EVENT {
    LIMIT = 12,
    PAGE = 1,
    NEXT_PAGE_LIMIT = 8
}
